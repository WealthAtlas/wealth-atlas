# Dashboard 
1. The Asset value line chart should be w.r.t the amount invested instead of the actual value + IRR along all the timeline of the investment
2. The Dashboard should show indian currency

# Loan
1. Loan View has to be redesigned
2. Loan next payment is wrong
3. Interest rate is not correct
4. No need of the active status. 

# Goal
1. Track the goal progress considering the SIP of the Asset that is tagged along with the IRR rate of the assets
2. The amount field should be formatted
3. Inflated amount should be computed when creating the Goal
4. Show the goal progress while editing the Goal by allocating the asset so that we can visualize the impact of changes in real-time.

